/* Functionality to make, edit, and cancel a workout class booking;

The "make" will send a "post request" to the database to store the booked class information.
The "edit" will send a "put request" to the database to alter/update the data being stored for an already
booked workout class.
The "cancel" will delete an existing class booking and send a "delete request" to the
database to remove the data being stored for it. */